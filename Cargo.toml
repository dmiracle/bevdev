[package]
name = "bevdev"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"
rand = { version = "0.10.1", features = ["chacha"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
